 
## الگوریتم‌های درخت تصمیم (Decision Tree) و جنگل تصادفی (Random Forest)

---

# بخش اول: الگوریتم درخت تصمیم (Decision Tree)

---

## تعریف و ایده‌ی کلی درخت تصمیم

الگوریتم **درخت تصمیم** یکی از روش‌های یادگیری ماشین نظارت‌شده است که برای **طبقه‌بندی (Classification)** و **پیش‌بینی مقدار عددی (Regression)** استفاده می‌شود.

ایده‌ی اصلی آن شبیه به **فرآیند تصمیم‌گیری انسان** است؛ یعنی:
- با پرسیدن سؤال‌های پشت‌سرهم
- داده‌ها را به زیرمجموعه‌های ساده‌تر تقسیم می‌کند
- تا در نهایت به یک تصمیم نهایی برسد

درخت تصمیم داده‌ها را به‌صورت **ساختار درختی** نمایش می‌دهد که در آن:
- هر گره یک شرط است
- هر شاخه نتیجه‌ی آن شرط
- و هر برگ، پاسخ نهایی
  
![c573e3d2-d2a4-4183-a2b1-0630d2c1ecdd_720x405](https://github.com/user-attachments/assets/baf881e3-6da7-4f53-a777-5d51addcac77)



---

##  نحوه‌ی عملکرد درخت تصمیم (با مثال)

## مسئله
می‌خواهیم تصمیم بگیریم که آیا یک پیشنهاد شغلی را **قبول کنیم یا رد کنیم**.

## ویژگی‌ها (Features)
- Salary (حقوق): High / Medium / Low
- Distance (فاصله): Near / Far
- Environment (محیط کاری): Good / Bad
- Growth (امکان رشد): Yes / No

## برچسب خروجی (Label)
- Accept Job: Yes / No

---

## ساختار درخت تصمیم (نمایش متنی)

            [Salary = High ?]
               /          \
            Yes            No
            |              |
    [Environment = Good ?] [Growth = Yes ?]
         /        \            /        \
      Yes          No        Yes          No
      |             |          |            |
  Accept Job   [Distance ?] [Environment ?] Reject Job
                  /    \        /      \
               Near    Far    Good     Bad
                |       |       |        |
            Accept   Reject  Accept    Reject


           
📌 نکته مهم:  
الگوریتم **به‌صورت خودکار** ترتیب سؤال‌ها را تعیین می‌کند.

---

##  اجزای اصلی درخت تصمیم

درخت تصمیم از اجزای زیر تشکیل شده است:

### 🔹 ریشه (Root Node)
- اولین و مهم‌ترین ویژگی
- بیشترین قدرت تفکیک داده‌ها را دارد

### 🔹 گره داخلی (Internal Node)
- شامل شرط یا آزمون روی یک ویژگی
- باعث تقسیم داده‌ها می‌شود

### 🔹 شاخه (Branch)
- نتیجه‌ی اجرای شرط (مثلاً بله/خیر)

### 🔹 برگ (Leaf Node)
- خروجی نهایی الگوریتم
- شامل کلاس یا مقدار پیش‌بینی‌شده

---

## معیارهای انتخاب بهترین ویژگی

هدف الگوریتم این است که در هر مرحله **بهترین ویژگی برای تقسیم داده‌ها** را انتخاب کند.

### 🔹 Entropy
- معیاری برای اندازه‌گیری میزان بی‌نظمی داده‌ها
- هرچه Entropy کمتر باشد، داده‌ها خالص‌ترند

### 🔹 Information Gain
- میزان کاهش Entropy بعد از تقسیم داده‌ها
- ویژگی با بیشترین Information Gain انتخاب می‌شود

### 🔹 Gini Index
- معیار جایگزین Entropy
- بیشتر در پیاده‌سازی‌های عملی استفاده می‌شود

📌 هدف نهایی:
> بیشترین جداسازی کلاس‌ها با کمترین پیچیدگی

---

## مزایا و معایب درخت تصمیم

### ✅ مزایا
- ساده و قابل درک
- قابلیت تفسیر بالا
- نیاز نداشتن به نرمال‌سازی داده‌ها
- قابل استفاده برای داده‌های عددی و غیرعددی

### ❌ معایب
- مستعد **overfitting**
- حساس به نویز
- ناپایداری در برابر تغییر داده‌ها

---

## کنترل پیچیدگی درخت (جلوگیری از Overfitting)

برای بهبود عملکرد:
- محدود کردن عمق درخت
- تعیین حداقل تعداد نمونه در برگ‌ها
- هرس کردن شاخه‌های غیرضروری (Pruning)

---

# بخش دوم: الگوریتم جنگل تصادفی (Random Forest)

---

## تعریف و ایده‌ی کلی جنگل تصادفی

الگوریتم **جنگل تصادفی** یک روش یادگیری **Ensemble** است که از ترکیب چندین درخت تصمیم استفاده می‌کند.

به‌جای اعتماد به یک مدل:
- چندین مدل مستقل ساخته می‌شود
- نتایج آن‌ها با هم ترکیب می‌شود

📌 فلسفه اصلی:
> ترکیب چند مدل ضعیف → یک مدل قوی


![bagging](https://github.com/user-attachments/assets/3b30005f-367f-4d76-b7fc-fb3a154f247e)
در تصویر بالا، ۹ متغیر با ۹ رنگ نمایش داده شده‌اند. در هر تقسیم، یک زیرمجموعه از متغیرها به صورت تصادفی از ۹ متغیر اصلی نمونه‌برداری می‌شوند. در این زیرمجموعه، الگوریتم بهترین متغیر را برای تقسیم انتخاب می‌کند. اندازه زیرمجموعه برابر با جذر تعداد متغیرهای اصلی تنظیم شده است. از این رو، در مثال ما، این عدد ۳ است.




---

## نحوه‌ی عملکرد جنگل تصادفی (با مثال)

فرض کنید برای تصمیم‌گیری مهم:
- از یک نفر نظر بپرسید → احتمال خطا زیاد است
- از صد نفر بپرسید → تصمیم قابل اعتمادتر می‌شود

در Random Forest:
1. چندین درخت تصمیم ساخته می‌شود
2. هر درخت:
   - داده‌های تصادفی متفاوتی می‌بیند
   - ویژگی‌های متفاوتی بررسی می‌کند
3. هر درخت یک خروجی تولید می‌کند
4. خروجی نهایی با رأی‌گیری یا میانگین‌گیری تعیین می‌شود

---

## مراحل ساخت Random Forest

1. نمونه‌گیری تصادفی از داده‌ها (Bootstrap)
2. انتخاب تصادفی ویژگی‌ها در هر تقسیم
3. ساخت درخت‌های مستقل
4. تجمیع نتایج نهایی

---

## مزایا و معایب جنگل تصادفی

### ✅ مزایا
- دقت بالا
- کاهش چشمگیر overfitting
- عملکرد مناسب روی داده‌های پیچیده
- مقاوم در برابر نویز

### ❌ معایب
- مصرف حافظه و زمان بیشتر
- عدم شفافیت در تصمیم‌گیری
- تفسیرپذیری پایین

---

## پارامترهای مهم در Random Forest

- تعداد درخت‌ها (n_estimators)
- عمق هر درخت
- تعداد ویژگی‌ها در هر تقسیم
- حداقل نمونه در برگ‌ها

---

# بخش سوم: مقایسه و جمع‌بندی

---

## مقایسه درخت تصمیم و جنگل تصادفی

| معیار | درخت تصمیم | جنگل تصادفی |
|-----|-----------|------------|
| ساختار | تک‌درختی | چنددرختی |
| دقت | متوسط | بالا |
| overfitting | زیاد | کم |
| تفسیرپذیری | بسیار بالا | پایین |
| سرعت اجرا | بالا | کمتر |
| کاربرد عملی | آموزشی | صنعتی |

---

## انتخاب الگوریتم مناسب

### استفاده از درخت تصمیم:
- آموزش مفاهیم
- نیاز به توضیح تصمیم
- داده‌های ساده

### استفاده از جنگل تصادفی:
- پروژه‌های واقعی
- داده‌های نویزی
- تمرکز بر دقت مدل

---

## جمع‌بندی نهایی

- درخت تصمیم ساده، قابل فهم اما ناپایدار است  
- جنگل تصادفی دقیق، پایدار اما غیرقابل تفسیر است  
- Random Forest نسخه‌ی بهبودیافته‌ی Decision Tree محسوب می‌شود  
